package com.example.demo.entity;

public enum DisputeStatus {
    OPEN,        // mới mở, đang xử lý
    PENDING,     // chờ admin phản hồi
    RESOLVED,    // đã giải quyết
    REJECTED     // bị từ chối
}
