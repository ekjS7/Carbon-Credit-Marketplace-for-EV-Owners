server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://db:3306/appdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8&useUnicode=true
    username: appuser
    password: secret
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

logging:
  level:
    com.example.demo: INFO
    org.springframework.web: INFO
    org.flywaydb: INFO

# VNPay Configuration (Sandbox)
vnpay:
  tmn-code: XUFPX320  # Terminal ID từ VNPay Sandbox
  hash-secret: 216M8O2A3QL1FESRY97R2MY2MIY32G0O  # Secret Key từ VNPay Sandbox
  pay-url: https://sandbox.vnpayment.vn/paymentv2/vpcpay.html
  return-url: http://localhost:5173/vnpay-return
  ipn-url: http://host.docker.internal:8083/api/payment/vnpay/ipn  # Docker internal host
  version: 2.1.0
  command: pay
  curr-code: VND